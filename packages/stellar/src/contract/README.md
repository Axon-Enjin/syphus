# PaymentRegistry TypeScript bindings

Generated after deploy via:

```bash
PAYMENT_REGISTRY_CONTRACT_ID=C... packages/contracts/scripts/bindings.sh
```

The app uses hand-written helpers in `../soroban.ts` that call the same contract methods. Re-run bindings after contract interface changes.
